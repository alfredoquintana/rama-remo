export enum TipoCompetencia {
  REGATA = 'regata',
  ERGOMETRO = 'ergometro',
}

export enum OrigenCompetencia {
  MANUAL = 'manual',
  IMPORTADA = 'importada',
}

export enum EstadoCompetencia {
  BORRADOR = 'borrador',
  EN_TRABAJO = 'en_trabajo',
  CERRADA = 'cerrada',
}

export enum OrigenDatoPrueba {
  MANUAL = 'manual',
  IMPORTADA = 'importada',
  EDITADA = 'editada',
}

export enum EstadoInscripcionCompetencia {
  PRESUNTIVA = 'presuntiva',
  NOMINATIVA = 'nominativa',
}
